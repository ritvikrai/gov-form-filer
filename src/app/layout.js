import './globals.css'
export const metadata = { title: 'Gov Form Filer', description: 'Simplify government paperwork' }
export default function RootLayout({ children }) {
  return <html lang="en"><body>{children}</body></html>
}
